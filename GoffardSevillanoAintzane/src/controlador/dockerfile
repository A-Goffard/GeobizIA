# Imagen base de Python
FROM python:3.10-slim

# Directorio de trabajo
WORKDIR /app

# Copia los archivos de dependencias si existen
COPY requirements.txt ./

# Instala las dependencias
RUN pip install --no-cache-dir -r requirements.txt

# Copia el resto del código fuente
COPY . .

# Expone el puerto del backend (ajusta si usas otro framework o puerto)
EXPOSE 5000

# Comando por defecto (ajusta a tu punto de entrada real)
CMD ["python", "main.py"]